import { ElementStates } from "./element-states";

export type TStringElement = {
  value: string;
  state: ElementStates;
};

export type TSortingElement = {
  value: number;
  state: ElementStates;
};
